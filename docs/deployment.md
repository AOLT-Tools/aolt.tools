# Deployment

The canonical deployment guide is [`../DEPLOYMENT.md`](../DEPLOYMENT.md).

The repository root is no longer deployed. Each Vercel project uses an app-specific Root Directory (`apps/aol.guide` or `apps/seva.hub`) and therefore deploys only that app's serverless entrypoints.
